import React from "react";
import "./style.css";

export const Component3500 = ({ className, divClassName }) => {
  return (
    <div className={`component-3500 ${className}`}>
      <div className={`text-wrapper-4 ${divClassName}`}>ถัดไป</div>
    </div>
  );
};
